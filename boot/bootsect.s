!
!
!  /*?????????????򣬱???????פ???ڴ??̵ĵ?һ????????
		(??????????0  ?ŵ��?0  ??ͷ????һ??????)
		PC???ӵ?ROM BIOS?Լ??󣬽?BIOS  ???ص??ڴ?0x7C00??????ִ??*/
!
!
! ?/*  SYS_SIZE ??Ҫ???ص?ϵͳģ?鳤??*/
! SYS_SIZE is the number of clicks (16 bytes) to be loaded.
! 0x3000 is 0x30000 bytes = 196kB, more than enough for current
! versions of linux
!
!/**/
SYSSIZE = 0x3000
!
!	bootsect.s		(C) 1991 Linus Torvalds
!
! bootsect.s is loaded at 0x7c00 by the bios-startup routines, and moves
! iself out of the way to address 0x90000, and jumps there.
!
! It then loads 'setup' directly after itself (0x90200), and the system
! at 0x10000, using BIOS interrupts. 
!
! NOTE! currently system is at most 8*65536 bytes long. This should be no
! problem, even in the future. I want to keep it simple. This 512 kB
! kernel size should be enough, especially as this doesn't contain the
! buffer cache as in minix
!
! The loader has been made as simple as possible, and continuos
! read errors will result in a unbreakable loop. Reboot by hand. It
! loads pretty fast by getting whole sectors at a time whenever possible.

.globl begtext, begdata, begbss, endtext, enddata, endbss		!/*??????6  ??ȫ?ֱ?��*/
.text													!/*?ı???*/
begtext:
.data												!/*???ݶ?*/
begdata:
.bss													!/*δ??ʼ?????ݶ?*/
begbss:
.text													!/*???ݶ?*/

SETUPLEN = 4				! nr of setup-sectors	/*setup??????????*/
BOOTSEG  = 0x07c0			! original address of boot-sector		/*bootsect??ԭʼ??ַ*/
INITSEG  = 0x9000			! we move boot here - out of the way		/*??bootsect?Ƶ?????ܿ?*/
SETUPSEG = 0x9020			! setup starts here			/*setup?????????￪ʼ*/
SYSSEG   = 0x1000			! system loaded at 0x10000 (65536).		/*systemģ?????ص?0x10000??*/
ENDSEG   = SYSSEG + SYSSIZE		! where to stop loading		/*ֹͣ???صĶε?ַ*/

! ROOT_DEV:	0x000 - same type of floppy as boot.
!		0x301 - first partition on first drive etc
ROOT_DEV = 0x306


entry start			!/*??֪��?ӳ??򣬳?????start   ??ʼִ??*/
start:
	mov	ax,#BOOTSEG		!/*??ds  ?Ĵ?????Ϊ0x7C0*/
	mov	ds,ax
	mov	ax,#INITSEG
	mov	es,ax
	mov	cx,#256			!/*?ƶ?????ֵ*/
	sub	si,si				!/*Դ??ַds:si=0x07c0:0x0000*/
	sub	di,di				!/*Ŀ?ĵ?ַes:di=0x9000:0x0000*/
	rep					!/*?ظ?ִ?е?cx=0??*/
	movw				!/*??movs?????????ڴ?[si]???ƶ?cx ???ֵ?[di]??*/
	jmpi	go,INITSEG		!/*?μ???ת*/
	
!/*CPU????0X90000  ???Ĵ?????ִ??
!	Ҫ??sp  ָ??Զ????512  ?ֽ?ƫ?ƣ???Ϊ0x90200  ?????з???setup  ????*/
go:	mov	ax,cs
	mov	ds,ax
	mov	es,ax

!ϵͳ??ʼ????ʱʹ?õĶ?ջ
	
! put stack at 0x9ff00.		!/*??spָ??0x9ff00*/
	mov	ss,ax
	mov	sp,#0xFF00		! arbitrary value >>512

! load the setup-sectors directly after the bootblock.
! Note that 'es' is already set up.			!/*exҪָ??Ŀ?Ķε?ַ??0x9000*/

!/*????BIOS  ?ж?INT  0x13  ??setup  ģ???Ӵ??̵ڶ?????????ʼ?��?0x90200??ʼ????
!	?????ĸ???????????????λ?????����?????*/
load_setup:
	mov	dx,#0x0000		! drive 0, head 0
	mov	cx,#0x0002		! sector 2, track 0
	mov	bx,#0x0200		! address = 512, in INITSEG
	mov	ax,#0x0200+SETUPLEN	! service 2, nr of sectors
	int	0x13			! read it
	jnc	ok_load_setup		! ok - continue
	mov	dx,#0x0000
	mov	ax,#0x0000		! reset the diskette
	int	0x13
	j	load_setup

ok_load_setup:

!/*ȡ???????????Ĳ???*/
! Get disk drive parameters, specifically nr of sectors/track

	mov	dl,#0x00
	mov	ax,#0x0800		! AH=8 is get drive parameters
	int	0x13
	mov	ch,#0x00

!/*??ʾ??һ???????Ĳ???????cs  ?μĴ?????ָ?Ķ???
	ֻӰ????һ??????*/
	seg cs

!/*????ÿ?ŵ???????*/
	mov	sectors,cx
	mov	ax,#INITSEG
	mov	es,ax			!/*??Ϊ??????ȡ???̲????жϸ?д??es??ֵ??????Ҫ???¸Ļ?*/

! Print some inane message

!/*??ʾ??Ϣ"Loading system....."*/

	mov	ah,#0x03		! read cursor pos
	xor	bh,bh
	int	0x10
	
	mov	cx,#24
	mov	bx,#0x0007		! page 0, attribute 7 (normal)
	mov	bp,#msg1
	mov	ax,#0x1301		! write string, move cursor
	int	0x10

! ok, we've written the message, now
! we want to load the system (at 0x10000)

!/*????system  ģ?鵽0x10000??*/

	mov	ax,#SYSSEG
	mov	es,ax		! segment of 0x010000
	call	read_it
	call	kill_motor		!/*?ر????????????֪????????״̬*/

! After that we check which root-device to use. If the device is
! defined (!= 0), nothing is done and the given device is used.
! Otherwise, either /dev/PS0 (2,28) or /dev/at0 (2,8), depending
! on the number of sectors that the BIOS reports currently.

!/*????Ҫ???õĸ??豸*/

	seg cs
	mov	ax,root_dev		!/*?ж?508??509?ֽڴ??ĸ??豸?Ƿ񱻶???*/
	cmp	ax,#0
	jne	root_defined
	seg cs
	mov	bx,sectors
	mov	ax,#0x0208		! /dev/ps0 - 1.2Mb
	cmp	bx,#15
	je	root_defined		!/*???????ȣ?ax?о??????????????豸??*/
	mov	ax,#0x021c		! /dev/PS0 - 1.44Mb
	cmp	bx,#18
	je	root_defined
undef_root:				!/*???????ȣ????��?*/
	jmp undef_root
root_defined:
	seg cs
	mov	root_dev,ax		!/*???????????豸?ű??ֵ?root_dev?С?*/

! after that (everyting loaded), we jump to
! the setup-routine loaded directly after
! the bootblock:


!/*???г??򶼼??????ϣ???ת??????????bootsect??????setup?????У?
	??ת??0x9020:0000(setup.c??????ʼ??)ȥִ??*/
	jmpi	0,SETUPSEG			!/*???˱??????ͽ?????*/

! This routine loads the system at address 0x10000, making sure
! no 64kB boundaries are crossed. We try to load it as fast as
! possible, loading whole tracks whenever we can.
!
! in:	es - starting address segment (normally 0x1000)
!

/*???ӳ?????ϵͳģ?????ص??ڴ???ַ0x1000??????ȷ??û??û??Խ??*/
sread:	.word 1+SETUPLEN	! sectors read of current track
head:	.word 0			! current head
track:	.word 0			! current track

read_it:
	/*?????????Ķ?ֵ???????϶????????ݱ???????λ???ڴ???ַ64KB?ı߽翪ʼ????
		??????????ѭ??*/
	mov ax,es
	test ax,#0x0fff
die:	jne die			! es must be at 64kB boundary			/*esֵ????λ??64KB  ??ַ?߽紦*/
	xor bx,bx		! bx is starting address within segment		/*bx?Ƕ???ƫ??*/
rp_read:
	/*?????ж??Ƿ??Ѿ?????ȫ??????
		???????Ǿ???ת??ok1_read???ţ????? ??*/
	mov ax,es
	cmp ax,#ENDSEG		! have we loaded all yet?
	jb ok1_read
	ret
ok1_read:
	/*??????ȡȫ????δ?????????񳬹?64KB*/
	seg cs
	mov ax,sectors		/*ȡÿ?ŵ???????*/
	sub ax,sread			/*??ȡ??ǰ?ŵ??Ѷ???????*/
	mov cx,ax			/*??ǰδ?��ŵ???*/
	shl cx,#9				/**/
	add cx,bx			/*?˴β????󣬶??ڹ????????ֽ???*/
	jnc ok2_read			/*û?г?????ת??ok2_read*/
	je ok2_read			/**/
	xor ax,ax
	sub ax,bx
	shr ax,#9
ok2_read:
	/*?ôŵ??Ƿ???????δ??*/
	call read_track
	mov cx,ax
	add ax,sread
	seg cs
	cmp ax,sectors	/*??????????δ?��???????3*/	
	jne ok3_read
	/*ת????һ?ŵ?*/
	mov ax,#1
	sub ax,head		/*?жϵ?ǰ?ŵ?*/
	jne ok4_read		/*??????0  ??ͷ??????ȥ??1  ??ͷ???ϵ?????????*/
	inc track			/*????????һ?ŵ?*/
ok4_read:
	mov head,ax		/*???浱ǰ??ͷ*/
	xor ax,ax			/*?嵱ǰ?ŵ??Ѷ???????*/
ok3_read:
	/*??ǰ?ŵ��???δ??????*/
	mov sread,ax		/*???浱ǰ?ŵ??Ѷ???????*/
	shl cx,#9			/*?ϴ??Ѷ?*/
	add bx,cx		/*??????ǰ???????ݿ?ʼλ??*/
	jnc rp_read
	/*????һ??*/
	mov ax,es
	add ax,#0x1000		/*??һ??64KB  ?ڴ濪ʼ??*/
	mov es,ax
	xor bx,bx				/*?????ڿ?ʼƫ??ֵ*/
	jmp rp_read			/*??????*/

/*?��?ǰ?ŵ???ָ????ʼ????????????????*/
read_track:
	push ax
	push bx
	push cx
	push dx
	mov dx,track		/*ȡ??ǰ?ŵ��?*/
	mov cx,sread		/*ȡ??ǰ?ŵ??Ѷ???????*/
	inc cx			/*c1 = ??ʼ??????*/
	mov ch,dl		/*ch  =  ??ǰ?ŵ��?*/
	mov dx,head		/*ȡ??ǰ??ͷ??*/
	mov dh,dl		/*dh  =  ??ͷ??*/
	mov dl,#0		/*dl  =  ????????(0  ??ʾ??ǰA  ??????)*/
	and dx,#0x0100	/*??ͷ?Ų?????1*/
	mov ah,#2		/*ah  =  2???��??????????ܺ?*/
	int 0x13
	jc bad_rt			/*????*/
	pop dx
	pop cx
	pop bx
	pop ax
	ret
	
/*?��??̳??���ִ??????????λ????????????read_track  ??????*/
bad_rt:	mov ax,#0
	mov dx,#0
	int 0x13
	pop dx
	pop cx
	pop bx
	pop ax
	jmp read_track

/*
 * This procedure turns off the floppy drive motor, so
 * that we enter the kernel in a known state, and
 * don't have to worry about it later.
 */

 /*?ر???????????????ں˺?Ҳ??֪??????????״̬*/
kill_motor:
	push dx
	mov dx,#0x3f2		/*???????ƿ????????????Ĵ????˿ڣ?ֻд*/
	mov al,#0			/*A?????����ر?FDC   ??ֹDMA???ж????󣬹ر?????*/
	outb					/*??al   ????????????dx   ָ???˿?ȥ*/
	pop dx
	ret

sectors:
	.word 0				/*???ŵ?ǰ????ÿ?ŵ��???????*/

msg1:					/*????BIOS  ?ж???ʾ??Ϣ*/
	.byte 13,10			/*?س??????е?ASCII??*/
	.ascii "Loading system ..."
	.byte 13,10,13,10		/*??24  ??*/


/*??????????508  ??ʼ*/
.org 508
root_dev:
	.word ROOT_DEV		/*???Ÿ??ļ?ϵͳ???ڵ??豸??(init/main.c?л??õ?)*/

/*????????????Ч?????????ı?־
	????BIOS  ?еĳ???????????????ʱʶ??ʹ?ã?
	????λ????????????????��???ֽ?*/
boot_flag:
	.word 0xAA55

.text
endtext:
.data
enddata:
.bss
endbss:
